{{$proxy := .}}{{$clsName := $proxy.ClassName}}namespace {包名}
{
    public static class {{$clsName}}
    {   {{$proxy := .}}{{range $index, $value := $proxy.GetItems}}
        /// <summary>
        /// {{$value.Remark}}
        /// </summary>
        public const {{$value.Type}} {{$value.Name}} = {{$value.Value}};{{end}}
    }
}