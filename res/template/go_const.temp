{{- $proxy := .}}{{$clsName := $proxy.ClassName -}}
package {{$proxy.Namespace}}
const (
    {{- range $index, $value := $proxy.GetItems}}
    // {{$value.Remark}}
    {{$value.Name}} {{$value.Type}} = {{$value.Value}}
	{{- end}}
)